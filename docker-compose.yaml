version: '3.1'
services:
  staples:
    image: 'staples:dev'
    container_name: 'staples'
    restart: always
    privileged: true
    ports:
      - 8888:8888
    volumes:
      - ./resource:/app/resource
      - ./log:/app/log
      - ./config/config.yaml:/app/config/config.yaml
      - /var/run/docker.sock:/var/run/docker.sock